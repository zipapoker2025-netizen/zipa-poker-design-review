/* The one place to put the Worker URL. Both the review page and the results page read it.
   Deploy the Worker first (see worker/DEPLOY.md), then paste its URL here and push. */
window.ZP_REVIEW_API = "https://zipa-review-api.zipapoker2025.workers.dev";
